<div class="contents">
  <div class="container">
    <%= form_with(model: [@property, @room, @approach],  local: true) do |form| %>
    <div class="input-form">
      <div class="remarks_room form">
        <div class="weight-bold-text">アプローチ</div>
          <%= form.text_area :content, placeholder: "アプローチ", rows: "10", size: "53x10" %>
        </div>
      <%= form.submit "登録する" %>
    </div>
    <% end %>
  </div>
</div>